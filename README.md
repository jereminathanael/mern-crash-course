# MERN Crash Course 🛒

A simple fullstack product management app built with the **MERN stack**. Supports full CRUD operations for products, with the frontend served by the backend in production mode.

---

## Tech Stack

| Layer | Technology |
|---|---|
| Frontend | React |
| Backend | Node.js, Express.js |
| Database | MongoDB (Atlas) |
| Runtime | Node.js |

---

## Features

- Get all products
- Create a new product
- Update an existing product
- Delete a product
- Production mode: backend serves the React frontend as static files

---

## Prerequisites

- [Node.js](https://nodejs.org) v18+
- [MongoDB Atlas](https://www.mongodb.com/atlas) account (or local MongoDB)

---

## Environment Setup

Create a `.env` file in the **root** directory:

```env
MONGO_URI=<your_mongodb_connection_string>
PORT=5000
```

> Get your connection string from MongoDB Atlas → your cluster → **Connect** → **Drivers**. It looks like:
> `mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/<dbname>?retryWrites=true&w=majority`

---

## How to Run

### Development

Run backend and frontend separately:

```bash
# Backend
npm install
npm run start

# Frontend (in a new terminal)
cd frontend
npm install
npm run dev
```

- Backend runs at `http://localhost:5000`
- Frontend runs at `http://localhost:5173`

### Production

Build the frontend first, then start the server. The backend will serve the React app as static files:

```bash
npm run build
npm run start
```

Open `http://localhost:5000` in your browser.

---

## API Routes

Base URL: `http://localhost:5000/api/products`

| Method | Endpoint | Description |
|---|---|---|
| GET | `/api/products` | Get all products |
| POST | `/api/products` | Create a new product |
| PUT | `/api/products/:id` | Update a product by ID |
| DELETE | `/api/products/:id` | Delete a product by ID |

---

## Project Structure

```
.
├── backend/
│   ├── config/
│   │   └── db.js                  # MongoDB connection
│   ├── controllers/
│   │   └── product.controller.js  # CRUD logic
│   ├── models/
│   │   └── product.model.js       # Models
│   ├── routes/
│   │   └── product.route.js       # Product routes
│   └── server.js                  # Express app entry point
├── frontend/                      # React frontend
│   └── dist/                      # Built output (generated by npm run build)
├── .env                           # Environment variables (not committed)
└── package.json
```